// Pulls in TypeScript typings for Vite-specific features (e.g. `import.meta.env`, asset imports).
// Without this triple-slash reference, the compiler may not know about Vite's client types.
/// <reference types="vite/client" />

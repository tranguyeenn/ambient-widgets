{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions shared by all widget webviews (lyric, calendar, …).",
  "windows": ["lyric", "calendar"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}
